import { NgDropDownDirective } from './ng-drop-down.directive';

describe('NgDropDownDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDropDownDirective();
    expect(directive).toBeTruthy();
  });
});
